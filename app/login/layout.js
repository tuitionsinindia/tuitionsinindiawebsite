export const metadata = {
    title: "Login — TuitionsInIndia",
    description: "Log in to your TuitionsInIndia account to manage your tutor profile, view student leads, or track your learning progress.",
    robots: {
        index: false,
        follow: false,
    }
};

export default function LoginLayout({ children }) {
    return children;
}
